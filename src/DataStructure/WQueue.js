// WQueue.js

requir('WS_LinkList');

// a queue that first-in-first-out follow the list 
class WS_Queue {
    // choose the WS_LinkList to save data
    // _dataStore =new WS_LinkList(); // private
    // 
    constructor() {
        //
    }
    // the first out the queue
    deQueue () {
        //
    }
    // add a item in the queue last
    enQueue (item) {
        //
    }
    // traverse fir the queue
    traverse (callback){
        //
    }
}

